# Base URL for the calculator API. Leave unset to use the same-origin
# reverse proxy (nginx in prod, vite dev proxy in dev) at /api/v1.
# Set to point at a real deployed backend, e.g.:
# VITE_API_BASE_URL=https://api.example.com/api/v1
VITE_API_BASE_URL=
